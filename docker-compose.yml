version: "3.8"

services:
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: "docker"
      POSTGRES_USER: "postgres"
      POSTGRES_DB: "extraclasse"
    ports:
      - "5433:5432"